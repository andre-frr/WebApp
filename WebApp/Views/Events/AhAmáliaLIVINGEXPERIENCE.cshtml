@model WebApp.Models.ViewModels.Events.EventsViewModel

@{
    ViewData["Title"] = Model.Evento.titulo;
}

<link rel="stylesheet" href="~/css/events.css">

<div class="event-container">
    <div class="event-header">
        <div class="image-wrapper">
            <img src="@Url.Content(Model.Evento.imagem)" alt="@Model.Evento.titulo">
        </div>
        <div class="event-details">
            <h1>@Model.Evento.titulo</h1>
            <p class="event-classification">
                Classificação: @Model.Evento.classificacao<br>
                Tipo: @Model.Evento.tipo
            </p>
            <div class="event-notes">
                <h4>Data e Hora: @Model.Evento.data_hora.ToString("dd MMM yyyy | HH:mm")</h4>
                <h4>Local: @Model.Evento.local_evento</h4>
                <h4>Preço: €@Model.Evento.preco.ToString("F2")</h4>
                <h4>Lotação Máxima: @Model.Evento.lotacao pessoas</h4>
            </div>
        </div>
    </div>

    <div class="event-description">
        <h2><strong>DESCRIÇÃO</strong></h2>
        <p>@Model.Evento.descricao</p>
    </div>

    <div class="button-wrapper">
        <a href="#" class="buy-button">ADICIONAR AO CARRINHO</a>
    </div>
</div>
